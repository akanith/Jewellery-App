export interface FAQItem {
  id: string;
  question: string;
  answer: string;
  category?: string;
}

export interface SupportInfo {
  phone: string;
  whatsappNumber: string;
  supportHours: string;
}
